**Istruzioni**

Nella directory 'storm-tape' avvia in locale il server col comando `./build/Debug/storm-tape-debug`

Dopodichè nella directory 'locust'
- attiva il virtual enviroment con `source ~/.venv/bin/activate`
- avvia lo script che gestisce i vari test di carico con `python3 benchmark.py`